# myPortfolio

Spring Boot app that will back the dynamic version of [sakib-khan.com](https://sakib-khan.com) — currently an early skeleton (application entry point + a `User` model), not yet in use.

## Tech stack

Java, Spring Boot, Gradle

## Getting started

```bash
./gradlew bootRun
```
